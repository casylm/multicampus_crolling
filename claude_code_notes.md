# Claude Code 환경 메모

## 1. 환경 사전 점검 결과 (2026-03-16)

| # | 항목 | 상태 | 비고 |
|---|------|------|------|
| 1 | Shell 환경 | ✅ 정상 | GNU bash 5.2.37, `/usr/bin/bash` |
| 2 | Git 설치 / 리포 초기화 | ⚠️ 부분 | Git 2.53.0 설치됨, 리포 미초기화 |
| 3 | 파일 읽기/쓰기 권한 | ✅ 정상 | CLAUDE_TEST 디렉토리 쓰기 가능 |
| 4 | 메모리 시스템 디렉토리 | ✅ 정상 | `~/.claude/projects/` 존재 |
| 5 | API 응답 | ✅ 정상 | 정상 작동 중 |

### 조치 필요 사항
Git 리포 미초기화 — 필요 시 아래 실행:
```bash
git init          # 현재 디렉토리를 Git 리포로 초기화
git add .         # 모든 파일을 스테이징에 추가
git commit -m "initial commit"  # 첫 커밋 생성
```

---

## 2. 터미널 vs IDE 작업 분리 기준

| # | 작업 | 권장 도구 | 이유 |
|---|------|-----------|------|
| 1 | Git 커밋/푸시 | 터미널 | 플래그 조합(`--amend`, `rebase -i`)이 GUI보다 빠름 |
| 2 | Diff 리뷰 | IDE (GitLens) | 인라인 blame, 줄 단위 이력, PR 코멘트 연동 |
| 3 | 패키지 설치 | 터미널 | 실시간 로그·에러 확인, 버전 플래그 조합 필요 |
| 4 | 리팩터링 (변수명·심볼 이동) | IDE | F2 rename이 AST 기반으로 참조 전체 변경; `sed`는 사이드이펙트 위험 |
| 5 | 로그 모니터링 / 서버 실행 | 터미널 (분할 패널) | 장시간 프로세스는 터미널 고정 후 IDE와 병렬 사용 |
| 6 | 멀티파일 검색·치환 | IDE (`Ctrl+Shift+H`) | 미리보기 + 선택적 적용 가능; `grep`+`sed`는 롤백 없음 |

**기본 원칙**: 선택적 적용·시각 확인 → IDE / 자동화·플래그 조합 → 터미널
